# Adaptive-algorithm-for-hybrid-system

ShieldSandbox.jl now uses JLD2 to speed up prototyping the shield algorithm. Be sure to use fresh_grid = true initially to compute the grid that is later serialized.